{
  "$schema": "https://aka.ms/codetour-schema",
  "title": "14. Serverless Endpoint & Router Architecture",
  "steps": [
    {
      "file": "scripture-habit/api/api.ts",
      "description": "Welcome to the Serverless Endpoint & Router Architecture Tour! Here we explore how the Express app handles routing inside a single serverless endpoint on Vercel. We first load routers and configure Sentry Diagnostics.",
      "line": 23
    },
    {
      "file": "scripture-habit/api/api.ts",
      "description": "Here is our dynamic CORS gatekeeper checking origins. For local testing, we regex-match standard HTTP localhost ports dynamically.",
      "line": 43
    },
    {
      "file": "scripture-habit/api/api.ts",
      "description": "Here is path normalization. Vercel routes sometimes append trailing slashes (e.g. `/api/auth/` instead of `/api/auth`). This middleware strips it internally to prevent duplicate endpoint configurations.",
      "line": 60
    },
    {
      "file": "scripture-habit/vercel.json",
      "description": "In `vercel.json`, we instruct Vercel to route all `/api/(.*)` patterns directly to our singular Express entry endpoint `/api/api` via the `rewrites` block, allowing Express to take full control of routing.",
      "line": 92
    }
  ]
}
